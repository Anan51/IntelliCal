# All keys are optional — the app runs in demo mode without them.

# Google Calendar two-way sync (OAuth 2.0)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Geocoding + Distance Matrix (travel times)
GOOGLE_MAPS_API_KEY=

# Optional LLM assist for syllabus parsing (regex fallback used when absent)
OPENAI_API_KEY=
